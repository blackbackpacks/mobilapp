window.onload = function(){
    leftSwipe()
    rightSwipe()
}

var leftSwipe = function(){
    var leftBox = document.querySelector('.left-navbar')
    var childBox = leftBox.querySelector('ul')  
    
    childBox.addEventListener('touchstart',function(e){})
    childBox.addEventListener('touchstart',function(e){})
    childBox.addEventListener('touchstart',function(e){})
}